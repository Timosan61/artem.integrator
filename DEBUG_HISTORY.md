# 🐛 Debug History - Artyom Integrator

> База знаний по решенным проблемам и ошибкам. Каждая запись - это урок для будущих проектов.

## 📋 Как использовать этот файл

1. **При возникновении сложной ошибки** - документируйте процесс решения
2. **При повторении похожей проблемы** - ищите здесь решение через Ctrl+F
3. **Перед релизом** - проверьте чек-лист типичных проблем
4. **Для обучения** - изучайте паттерны ошибок

## 🔖 Индекс категорий

- [Telegram API](#telegram-api)
- [Railway Deploy](#railway-deploy)  
- [Voice Service](#voice-service)
- [Webhook](#webhook)
- [Database](#database)
- [Integration](#integration)
- [MCP Integration](#mcp-integration)

## ⚡ Быстрый чек-лист диагностики

### При ошибках Telegram:
- [ ] Проверить форматирование HTML (< > & требуют экранирования)
- [ ] Проверить длину сообщения (макс 4096 символов)
- [ ] Проверить права бота в чате
- [ ] Проверить токен бота

### При проблемах с Railway:
- [ ] Проверить логи сборки `railway logs --service web`
- [ ] Проверить переменные окружения
- [ ] Проверить Dockerfile и пути
- [ ] Проверить requirements.txt

### При проблемах с Voice:
- [ ] Проверить VOICE_ENABLED в конфиге
- [ ] Проверить наличие voice/ директории
- [ ] Проверить OpenAI API ключ
- [ ] Проверить размер аудио файла

---

# 🔍 Паттерны частых проблем

## Telegram API
- **HTML parsing**: Всегда экранировать <, >, &
- **Лимиты**: Сообщение макс 4096, имя файла макс 64 символа
- **Media**: Фото до 10MB, аудио до 50MB
- **Rate limits**: 30 сообщений в секунду в группу

## Railway Deploy
- **Environment**: Переменные из dashboard, не из .env
- **Paths**: Все пути должны быть относительными
- **Memory**: Ограничение 512MB на бесплатном плане
- **Dockerfile**: Обязательно копировать все нужные директории

## Async/Await
- **Забытый await**: Самая частая проблема
- **Синхронный код в async**: Блокирует event loop
- **Fire and forget**: Используйте asyncio.create_task()

---

# 📎 Полезные команды для отладки

```bash
# Railway логи
railway logs --service web
railway logs --service web --since 30m

# Поиск в истории
grep -n "HTML" DEBUG_HISTORY.md
grep -n "voice" DEBUG_HISTORY.md

# Git история исправлений
git log --grep="fix:" --oneline

# Поиск TODO в коде
grep -r "TODO\|FIXME\|BUG" bot/
```

---

> 💡 **Совет**: Ctrl+F в этом файле - ваш лучший друг при отладке!

## 2025-01-21 - MCP не включается несмотря на MCP_ENABLED=true

### Описание ошибки
MCP функциональность не активируется, хотя MCP_ENABLED=true установлен в .env файле.

### Симптомы
- В логах: `'mcp': '❌'` при запуске бота
- Все MCP команды отвечают: "не могу выполнить команду"
- `grep MCP .env` показывает `MCP_ENABLED=true`
- В конфигурации MCP показывается как отключенный

### Причина
MCP читает переменную окружения напрямую из environment, а не из .env файла:
```python
enabled = os.getenv('MCP_ENABLED', 'false').lower() == 'true'
```

### Решение
1. Экспортировать переменную перед запуском:
   ```bash
   export MCP_ENABLED=true
   python webhook.py
   ```

2. Или использовать python-dotenv для загрузки .env файла

### Важные уроки
1. **Railway использует переменные из dashboard, не из .env**
2. **Локально нужно явно экспортировать переменные**
3. **Проверять конфигурацию в логах при запуске**

### Затронутые файлы
- `bot/core/config.py` - читает MCP_ENABLED из environment
- `.env` - содержит MCP_ENABLED=true но не загружается автоматически

## 📝 ШАБЛОН ДЛЯ НОВОЙ ЗАПИСИ

```markdown
## ГГГГ-ММ-ДД - Краткое описание проблемы

### Описание ошибки
Что именно не работало

### Симптомы
- Как проявлялась ошибка
- Что видел пользователь
- Что было в логах

### Попытки исправления
#### Попытка 1: Название метода
**Проблема**: Что именно было не так
**Действие**: Что сделали
**Результат**: ✅/❌ Помогло или нет

#### Попытка 2: Название метода
**Проблема**: Что думали исправить
**Действие**: Что изменили
**Результат**: ✅/❌ Результат

### Финальное решение
Что в итоге помогло решить проблему

### Важные уроки
1. Ключевой урок для будущих проектов
2. Что важно проверять в первую очередь
3. Как избежать этой ошибки

### Затронутые файлы
- `path/to/file1.py` - что изменено
- `path/to/file2.py` - что изменено
```

---

# 🔍 Паттерны частых проблем

## Telegram API
- **HTML parsing**: Всегда экранировать <, >, &
- **Лимиты**: Сообщение макс 4096, имя файла макс 64 символа
- **Media**: Фото до 10MB, аудио до 50MB
- **Rate limits**: 30 сообщений в секунду в группу

## Railway Deploy
- **Environment**: Переменные из dashboard, не из .env
- **Paths**: Все пути должны быть относительными
- **Memory**: Ограничение 512MB на бесплатном плане
- **Dockerfile**: Обязательно копировать все нужные директории

## Async/Await
- **Забытый await**: Самая частая проблема
- **Синхронный код в async**: Блокирует event loop
- **Fire and forget**: Используйте asyncio.create_task()

---

# 📎 Полезные команды для отладки

```bash
# Railway логи
railway logs --service web
railway logs --service web --since 30m

# Поиск в истории
grep -n "HTML" DEBUG_HISTORY.md
grep -n "voice" DEBUG_HISTORY.md

# Git история исправлений
git log --grep="fix:" --oneline

# Поиск TODO в коде
grep -r "TODO\|FIXME\|BUG" bot/
```

---

> 💡 **Совет**: Ctrl+F в этом файле - ваш лучший друг при отладке!

## 2025-01-21 - MCP не включается несмотря на MCP_ENABLED=true

### Описание ошибки
MCP функциональность не активируется, хотя MCP_ENABLED=true установлен в .env файле.

### Симптомы
- В логах: `'mcp': '❌'` при запуске бота
- Все MCP команды отвечают: "не могу выполнить команду"
- `grep MCP .env` показывает `MCP_ENABLED=true`
- В конфигурации MCP показывается как отключенный

### Причина
MCP читает переменную окружения напрямую из environment, а не из .env файла:
```python
enabled = os.getenv('MCP_ENABLED', 'false').lower() == 'true'
```

### Решение
1. Экспортировать переменную перед запуском:
   ```bash
   export MCP_ENABLED=true
   python webhook.py
   ```

2. Или использовать python-dotenv для загрузки .env файла

### Важные уроки
1. **Railway использует переменные из dashboard, не из .env**
2. **Локально нужно явно экспортировать переменные**
3. **Проверять конфигурацию в логах при запуске**

### Затронутые файлы
- `bot/core/config.py` - читает MCP_ENABLED из environment
- `.env` - содержит MCP_ENABLED=true но не загружается автоматически

# 📚 История решенных проблем

## 2025-01-21 - MCP команды распознаются, но не выполняются

### Описание ошибки
MCP включен в конфигурации ('mcp': '✅'), команды добавлены в инструкцию, но бот отвечает на них как на обычный текст, не выполняя MCP функции.

### Симптомы
- В конфигурации: `'mcp': '✅'` 
- Команды `/mcp status`, `/db SELECT version()` и др. приходят в логи
- Бот отвечает: "не могу выполнить команду" или дает общую информацию
- MCP сервисы существуют в коде, но не интегрированы в обработчик

### Попытки исправления

#### Попытка 1: Добавление переменных MCP серверов в .env
**Проблема**: MCP показывался как отключенный из-за отсутствия настроенных серверов
**Действие**: Добавили SUPABASE_ENABLED=true, DIGITALOCEAN_ENABLED=true, CONTEXT7_ENABLED=true
**Результат**: ✅ MCP стал показываться как включенный

#### Попытка 2: Добавление MCP команд в инструкцию
**Проблема**: AI не знал о существовании MCP команд
**Действие**: Обновили data/instruction.json с описанием всех MCP команд
**Результат**: ❌ AI знает о командах, но не может их выполнять

### Причина проблемы
MCP модули существуют (mcp_service.py, mcp_manager.py), но не интегрированы в основной поток обработки сообщений. Бот использует только AI для генерации ответов, без специального обработчика для MCP команд.

### Решение (требует реализации)
Необходимо:
1. Создать обработчик MCP команд в bot/webhook/handlers.py
2. Интегрировать MCPManager в основной agent
3. Добавить роутинг команд к соответствующим MCP сервисам

### Важные уроки
1. **Наличие кода != работающая функция** - MCP модули есть, но не подключены
2. **Инструкция для AI != исполняемый код** - обновление инструкции не дает AI возможность выполнять команды
3. **Всегда проверять интеграцию** - новые функции должны быть интегрированы в основной поток

### Затронутые файлы
- `.env` - добавлены переменные MCP серверов
- `data/instruction.json` - добавлены описания MCP команд
- `bot/webhook/handlers.py` - требует добавления обработчика MCP (не реализовано)

## 2025-01-21 - HTML теги в сообщениях Telegram вызывают ошибку

### Описание ошибки
Telegram бот получал сообщения (видно в логах), но не отправлял ответы пользователям из-за ошибок HTML форматирования.

### Симптомы
- В логах: "✅ Response sent successfully" 
- Но в Telegram сообщения от бота не появлялись
- Ошибка: `Bad Request: can't parse entities: Unsupported start tag "запрос" at byte offset 164`
- Пользователь писал: "бот молчит", "я отправляю опять ничего не происходит"

### Попытки исправления

#### Попытка 1: Проверка секретного токена webhook
**Проблема**: `{"ok":false,"error":"Invalid secret token"}`
**Действие**: Добавили `TELEGRAM_WEBHOOK_SECRET=artyom_integrator_secret_2025` в переменные окружения
**Результат**: ✅ Webhook начал принимать запросы

#### Попытка 2: Исправление HTML форматирования
**Проблема**: `Bad Request: can't parse entities: Unsupported start tag "запрос" at byte offset 164`
**Действие**: В файле `bot/auth.py` заменили `<запрос>` на `&lt;запрос&gt;`
**Результат**: ✅ Ошибка устранена, но бот все еще не отвечал

#### Попытка 3: Очистка Python кэша
**Проблема**: Подозрение на кэшированные модули Python
**Действие**: 
```bash
find . -type d -name "__pycache__" -exec rm -rf {} +
PYTHONDONTWRITEBYTECODE=1 python webhook.py
```
**Результат**: ❌ Не помогло

#### Попытка 4: Временное отключение HTML форматирования
**Действие**: В `handlers.py` временно убрали `parse_mode='HTML'` для команды /start
**Результат**: ❌ Логи показывали отправку, но сообщения не доходили

#### Попытка 5: Создание автоматического тестера
**Действие**: Создали `auto_test_bot.py` для автоматической отправки команд и мониторинга логов
**Результат**: ✅ Помогло выявить, что бот отправляет ответы, но есть еще HTML ошибки

### Финальное решение
1. Исправили все HTML ошибки:
   - В `bot/auth.py`: `<запрос>` → `&lt;запрос&gt;`
   - В `bot/webhook/handlers.py`: `<url>` → `&lt;url&gt;`
2. Установили webhook вручную через curl с правильным URL
3. Добавили функцию экранирования HTML для всех текстов

### Важные уроки

1. **HTML форматирование в Telegram требует экранирования**
   - Все теги типа `<something>` должны быть заменены на `&lt;something&gt;`
   - Telegram API строго проверяет HTML и отклоняет сообщения с ошибками
   - Порядок замены важен: сначала &, потом < и >

2. **Логи могут вводить в заблуждение**
   - "Response sent successfully" означает только успешный вызов API
   - Не гарантирует, что сообщение было принято Telegram
   - Всегда проверять конечный результат

3. **Автоматическое тестирование критично**
   - Создание тестера помогло быстро находить проблемы
   - Мониторинг логов в реальном времени ускоряет отладку
   - Автоматизация воспроизведения ошибок экономит время

4. **Переменные окружения для webhook**
   - `TELEGRAM_WEBHOOK_SECRET` обязателен для безопасности
   - `TELEGRAM_WEBHOOK_URL` должен передаваться при запуске
   - Проверять все переменные перед запуском

### Затронутые файлы
- `bot/auth.py` - исправлены HTML теги в тексте
- `bot/webhook/handlers.py` - исправлены HTML теги в ответах
- `auto_test_bot.py` - создан для автоматического тестирования

---

## MCP Integration

### 2025-01-21 - MCP интеграция через Claude Code SDK

#### Описание ошибки
MCP команды не работали несмотря на включенный MCP_ENABLED=true. Команды принимались ботом, но обрабатывались как обычные AI запросы, а не MCP функции.

#### Симптомы
- MCP показывался как disabled в статусе, хотя MCP_ENABLED=true
- Команды /mcp, /db, /docs обрабатывались AI, а не MCP
- В логах не было вызовов MCP функций
- Сообщение "изучи документацию... возможно проблема в том что и агент пытается подключиться к mcp"

#### Попытки исправления

##### Попытка 1: Добавление переменных окружения
**Проблема**: MCP показывался как disabled
**Действие**: Добавили SUPABASE_ENABLED, DIGITALOCEAN_ENABLED, CONTEXT7_ENABLED
**Результат**: ✅ MCP показался как enabled, но команды все еще не работали

##### Попытка 2: Прямая интеграция MCP
**Проблема**: Попытка прямого подключения к MCP серверам
**Действие**: Создание mcp_service.py с прямыми вызовами
**Результат**: ❌ MCP работает только через Claude Code, не напрямую

##### Попытка 3: Claude Code SDK
**Проблема**: SDK имеет проблемы совместимости с зависимостями
**Действие**: Установка claude-code-sdk==0.0.14
**Результат**: ❌ Конфликты с anyio версиями, SDK использует CLI с неправильными параметрами

#### Финальное решение
1. Создан `bot/services/claude_code_service.py` с поддержкой эмуляции
2. Интегрирован в `bot/webhook/handlers.py` для админских команд
3. Добавлена проверка MCP_ENABLED и прав администратора
4. Реализована эмуляция основных MCP команд для тестирования
5. Создана конфигурация в `data/mcp-servers.json`

#### Важные уроки
1. **MCP доступен только через Claude Code** - прямое подключение невозможно
2. **Claude Code SDK имеет проблемы совместимости** - нужна правильная версия или эмуляция
3. **Режим эмуляции полезен для тестирования** - позволяет проверить интеграцию без SDK
4. **Важно добавлять все переменные окружения** - ANTHROPIC_ENABLED, ANTHROPIC_API_KEY обязательны
5. **MCP команды только для админов** - требуется проверка прав

#### Затронутые файлы
- `bot/services/claude_code_service.py` - новый сервис для MCP через Claude Code
- `bot/webhook/handlers.py` - интеграция MCP команд
- `data/mcp-servers.json` - конфигурация MCP серверов
- `bot/core/config.py` - добавлены свойства для MCP серверов
- `requirements.txt` - добавлен claude-code-sdk

---

# 🔍 Паттерны частых проблем

## Telegram API
- **HTML parsing**: Всегда экранировать <, >, &
- **Лимиты**: Сообщение макс 4096, имя файла макс 64 символа
- **Media**: Фото до 10MB, аудио до 50MB
- **Rate limits**: 30 сообщений в секунду в группу

## Railway Deploy
- **Environment**: Переменные из dashboard, не из .env
- **Paths**: Все пути должны быть относительными
- **Memory**: Ограничение 512MB на бесплатном плане
- **Dockerfile**: Обязательно копировать все нужные директории

## Async/Await
- **Забытый await**: Самая частая проблема
- **Синхронный код в async**: Блокирует event loop
- **Fire and forget**: Используйте asyncio.create_task()

---

# 📎 Полезные команды для отладки

```bash
# Railway логи
railway logs --service web
railway logs --service web --since 30m

# Поиск в истории
grep -n "HTML" DEBUG_HISTORY.md
grep -n "voice" DEBUG_HISTORY.md

# Git история исправлений
git log --grep="fix:" --oneline

# Поиск TODO в коде
grep -r "TODO\|FIXME\|BUG" bot/
```

---

> 💡 **Совет**: Ctrl+F в этом файле - ваш лучший друг при отладке!

## 2025-01-21 - MCP не включается несмотря на MCP_ENABLED=true

### Описание ошибки
MCP функциональность не активируется, хотя MCP_ENABLED=true установлен в .env файле.

### Симптомы
- В логах: `'mcp': '❌'` при запуске бота
- Все MCP команды отвечают: "не могу выполнить команду"
- `grep MCP .env` показывает `MCP_ENABLED=true`
- В конфигурации MCP показывается как отключенный

### Причина
MCP читает переменную окружения напрямую из environment, а не из .env файла:
```python
enabled = os.getenv('MCP_ENABLED', 'false').lower() == 'true'
```

### Решение
1. Экспортировать переменную перед запуском:
   ```bash
   export MCP_ENABLED=true
   python webhook.py
   ```

2. Или использовать python-dotenv для загрузки .env файла

### Важные уроки
1. **Railway использует переменные из dashboard, не из .env**
2. **Локально нужно явно экспортировать переменные**
3. **Проверять конфигурацию в логах при запуске**

### Затронутые файлы
- `bot/core/config.py` - читает MCP_ENABLED из environment
- `.env` - содержит MCP_ENABLED=true но не загружается автоматически

## 2025-01-21 - Ошибка установки webhook URL

### Описание ошибки
При запуске бота webhook URL не передавался корректно.

### Симптомы
- В логах: `Bad Request: invalid webhook URL specified`
- Webhook URL показывался как пустой: `🌐 Webhook URL: `

### Решение
Установка webhook вручную через API:
```bash
curl -X POST "https://api.telegram.org/bot{TOKEN}/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://ngrok-url/webhook",
    "secret_token": "secret_token"
  }'
```

### Урок
Всегда проверять, что переменные окружения правильно экспортированы перед запуском.

---

# 🔍 Паттерны частых проблем

## Telegram API
- **HTML parsing**: Всегда экранировать <, >, &
- **Лимиты**: Сообщение макс 4096, имя файла макс 64 символа
- **Media**: Фото до 10MB, аудио до 50MB
- **Rate limits**: 30 сообщений в секунду в группу

## Railway Deploy
- **Environment**: Переменные из dashboard, не из .env
- **Paths**: Все пути должны быть относительными
- **Memory**: Ограничение 512MB на бесплатном плане
- **Dockerfile**: Обязательно копировать все нужные директории

## Async/Await
- **Забытый await**: Самая частая проблема
- **Синхронный код в async**: Блокирует event loop
- **Fire and forget**: Используйте asyncio.create_task()

---

# 📎 Полезные команды для отладки

```bash
# Railway логи
railway logs --service web
railway logs --service web --since 30m

# Поиск в истории
grep -n "HTML" DEBUG_HISTORY.md
grep -n "voice" DEBUG_HISTORY.md

# Git история исправлений
git log --grep="fix:" --oneline

# Поиск TODO в коде
grep -r "TODO\|FIXME\|BUG" bot/
```

---

> 💡 **Совет**: Ctrl+F в этом файле - ваш лучший друг при отладке!

## 2025-01-21 - MCP не включается несмотря на MCP_ENABLED=true

### Описание ошибки
MCP функциональность не активируется, хотя MCP_ENABLED=true установлен в .env файле.

### Симптомы
- В логах: `'mcp': '❌'` при запуске бота
- Все MCP команды отвечают: "не могу выполнить команду"
- `grep MCP .env` показывает `MCP_ENABLED=true`
- В конфигурации MCP показывается как отключенный

### Причина
MCP читает переменную окружения напрямую из environment, а не из .env файла:
```python
enabled = os.getenv('MCP_ENABLED', 'false').lower() == 'true'
```

### Решение
1. Экспортировать переменную перед запуском:
   ```bash
   export MCP_ENABLED=true
   python webhook.py
   ```

2. Или использовать python-dotenv для загрузки .env файла

### Важные уроки
1. **Railway использует переменные из dashboard, не из .env**
2. **Локально нужно явно экспортировать переменные**
3. **Проверять конфигурацию в логах при запуске**

### Затронутые файлы
- `bot/core/config.py` - читает MCP_ENABLED из environment
- `.env` - содержит MCP_ENABLED=true но не загружается автоматически