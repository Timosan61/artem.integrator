FROM artem-mcp-base:latest

# Копируем конфигурацию MCP сервера
COPY docker/mcp/servers/context7/ /app/

# Установка зависимостей для Context7 MCP
RUN npm install axios

# Переменные окружения
ENV MCP_SERVER_NAME=context7
ENV MCP_SERVER_PORT=3003

# Запуск MCP сервера
CMD ["node", "server.js"]