{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile",
    "dockerfilePath": "Dockerfile.final"
  },
  "deploy": {
    "startCommand": "python -m uvicorn webhook:app --host 0.0.0.0 --port $PORT --log-level info",
    "healthcheckPath": "/",
    "healthcheckTimeout": 120,
    "restartPolicyType": "on_failure"
  },
  "environments": {
    "production": {
      "variables": {
        "TELEGRAM_BOT_TOKEN": null,
        "WEBHOOK_SECRET_TOKEN": null
      }
    }
  }
}