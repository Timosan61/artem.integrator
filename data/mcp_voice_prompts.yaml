# Конфигурация голосовых команд для MCP
# Этот файл определяет, как голосовые команды транслируются в MCP вызовы

voice_commands:
  system_prompt: |
    Ты голосовой ассистент для управления инфраструктурой через естественный язык.
    Определи намерение пользователя и выполни соответствующее действие.
    ВАЖНО: Верни ответ в дружелюбном формате для Telegram с использованием эмодзи и понятного языка.
    
  scenarios:
    # DigitalOcean Apps
    - name: "applications"
      triggers:
        - "приложения"
        - "мои приложения" 
        - "список приложений"
        - "покажи приложения"
        - "какие у меня приложения"
        - "приложения в DigitalOcean"
        - "apps"
        - "applications"
      action: "mcp__digitalocean__list_apps"
      parameters: 
        query: {}
      response_template: |
        📁 **Ваши DigitalOcean приложения:**
        {apps_list}
        
    # DigitalOcean Droplets (когда будет доступно)
    - name: "droplets"
      triggers:
        - "дроплеты"
        - "дроплет"
        - "сервера"
        - "серверы"
        - "виртуальные машины"
        - "VPS"
        - "droplets"
        - "servers"
      action: "mcp__digitalocean__list_droplets"
      fallback_message: |
        💧 К сожалению, управление дроплетами пока не поддерживается в текущей версии.
        
        Доступные команды:
        📱 Приложения - "покажи мои приложения"
        🗄️ Базы данных - "какие у меня базы данных"
        🚀 Деплойменты - "история деплойментов"
        
    # DigitalOcean Databases
    - name: "databases"
      triggers:
        - "базы данных"
        - "базы"
        - "БД"
        - "database"
        - "databases"
        - "базы в DigitalOcean"
      action: "mcp__digitalocean__list_databases_cluster"
      parameters:
        query: {}
      response_template: |
        🗄️ **Ваши базы данных:**
        {databases_list}
        
    # DigitalOcean Deployments
    - name: "deployments"
      triggers:
        - "деплойменты"
        - "развертывания"
        - "история деплоев"
        - "история развертываний"
        - "deployments"
        - "деплои"
      action: "mcp__digitalocean__list_deployments"
      parameters:
        path:
          app_id: "{app_id}"
        query: {}
      requires_context: true
      context_prompt: "Для какого приложения показать деплойменты?"
      response_template: |
        🚀 **История деплойментов:**
        {deployments_list}
        
    # Specific App Info
    - name: "app_info"
      triggers:
        - "информация о приложении"
        - "детали приложения"
        - "приложение"
        - "app info"
        - "application details"
      action: "mcp__digitalocean__get_app"
      parameters:
        path:
          id: "{app_id}"
        query:
          name: "{app_name}"
      requires_context: true
      context_extraction:
        - pattern: "приложени[ие] (.+)"
          group: 1
          variable: "app_name"
      response_template: |
        📦 **Информация о приложении:**
        {app_details}
        
    # Database Options
    - name: "database_options"
      triggers:
        - "какие базы данных доступны"
        - "варианты баз данных"
        - "типы баз данных"
        - "database options"
        - "available databases"
      action: "mcp__digitalocean__get_database_options"
      parameters: {}
      response_template: |
        🗂️ **Доступные типы баз данных:**
        {database_options}
        
    # Instance Sizes
    - name: "instance_sizes"
      triggers:
        - "размеры инстансов"
        - "доступные размеры"
        - "instance sizes"
        - "размеры серверов"
      action: "mcp__digitalocean__list_instance_sizes"
      parameters: {}
      response_template: |
        📏 **Доступные размеры инстансов:**
        {instance_sizes}
        
    # App Regions
    - name: "regions"
      triggers:
        - "регионы"
        - "доступные регионы"
        - "regions"
        - "локации"
        - "дата центры"
      action: "mcp__digitalocean__list_app_regions"
      parameters: {}
      response_template: |
        🌍 **Доступные регионы:**
        {regions_list}
        
    # Supabase Projects
    - name: "supabase_projects"
      triggers:
        - "проекты Supabase"
        - "проекты в Supabase"
        - "Supabase projects"
        - "список проектов Supabase"
      action: "mcp__supabase__list_projects"
      parameters: {}
      response_template: |
        🚀 **Ваши Supabase проекты:**
        {projects_list}
        
  # Ответ по умолчанию, если не распознана команда
  default_response: |
    🤔 Не совсем понял ваш запрос. 
    
    Вот что я умею:
    📱 **Приложения** - "покажи мои приложения"
    🗄️ **Базы данных** - "какие у меня базы данных"
    🚀 **Деплойменты** - "история деплойментов"
    🌍 **Регионы** - "доступные регионы"
    
    Попробуйте сформулировать запрос иначе.
    
  # Настройки обработки
  processing:
    # Минимальная уверенность для сопоставления триггера (0-1)
    min_confidence: 0.7
    # Использовать нечеткое сопоставление
    fuzzy_matching: true
    # Язык по умолчанию
    default_language: "ru"